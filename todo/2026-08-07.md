# 2026-08-07

## What We Built

- Prepared Presenter as a standalone, reusable presentation framework that can be published without organization-specific content.
- Added a working lint configuration so code-quality checks can run consistently.

## What We Fixed

- Removed hardcoded corporate branding, personnel imagery, customer/demo media, and presentation-specific references from the publishable project.
- Strengthened credential protection so environment files and common private-key formats stay outside version control.
- Restored a missing presentation metadata type used by per-deck branding.
- Resolved the lint errors that blocked the standard project check.

## Key Decisions

- Kept the original organization/customer asset bundle recoverable outside the project instead of permanently deleting it.
- Replaced the organization-specific default theme with a generic Spectrum preset while preserving the same visual palette.
- Kept newer React migration rules out of the lint gate until the existing component library can be migrated intentionally.

## Breaking Changes

- The default built-in preset identifier changed. Previously saved local theme selections may fall back to the new generic default.
